<%= form_with(model: diary) do |form| %>
  <% if diary.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(diary.errors.count, "error") %> prohibited this diary from being saved:</h2>

      <ul>
        <% diary.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label "タイトル", style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label "本文", style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.submit "更新する" %>
  </div>
<% end %>
